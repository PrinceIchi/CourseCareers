const matrix = [];

for (let i = 0; i < 6; i++) {
    const numbers = [];

    
        for (let j = i * 2; j < i * 2 + 2; j++) {
            numbers.push(j + 1);
            for (let k = 0; k < 6; k++) {
                const for3 = [];
        
        }
    }
    matrix.push(numbers);
}

console.log(matrix);